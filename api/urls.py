from django.urls import path
# from .views import User

urlpatterns = [
    # path('auth/register', User.RegisterAPIView.as_view(), name='Register'),
    # path('auth/login', User.LoginAPIView.as_view(), name='Login'),
]